# --- secrets (NEVER commit) ---
.env
token.txt

# --- runtime state ---
session.id
effort.level
cwd.path
BLOCKED.flag
SLEEP.flag

# --- logs ---
claudegram.log*
gui-stderr.log

# --- python / venv ---
.venv/
__pycache__/
*.pyc

# HARNESS IPC drop dir (runtime messages)
outbox/
inbox/
